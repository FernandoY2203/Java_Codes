spring:
  h2:
    console:
      enabled: true
      path: /h2
      settings:
        trace: false
        web-allow-others: false
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format-sql: true


        spring:
          datasource:
            url: jdbc:postgresql://postgres.railway.internal:5432/railway
            username: postgres
            password: bpsPkroAAwLhRNkwViglzKsEvOdcGtyM
            driver-class-name: org.postgresql.Driver
          jpa:
            open-in-view: false
            hibernate:
              ddl-auto: create
              dialect: org.hibernate.dialect.PostgreSQLDialect
